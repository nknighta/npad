const files_ui = document.getElementById('menu-ui-files');

let isMenuOpend = false;
const menuElement = document.createElement("div");

const menuElementTextTest = document.createTextNode("OMG");

menuElement.appendChild(menuElementTextTest);
